{
  "name": "cc-plugin-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "A curated Claude Code plugin marketplace for managing multiple plugins in a single repository.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "npm run validate:marketplace && npm run validate:claude",
    "validate:claude": "claude plugin validate .",
    "validate:claude:strict": "claude plugin validate . --strict",
    "validate:marketplace": "node scripts/validate-marketplace.mjs"
  }
}
